package com.banquito.platform.notification.domain.repository;

import com.banquito.platform.notification.domain.model.AuditoriaNotificationEvento;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AuditoriaNotificationEventoRepository extends JpaRepository<AuditoriaNotificationEvento, Long> { }
